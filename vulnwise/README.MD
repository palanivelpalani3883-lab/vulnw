## Run

Windows: `run.ps1`; macOS/Linux: `./run.sh`.

If PowerShell reports `ObjectNotFound: (streamlit:String)`, the virtual environment is not activated or its Scripts directory is not on PATH; run the launcher instead.
